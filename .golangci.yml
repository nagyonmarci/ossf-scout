version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
  settings:
    errcheck:
      check-type-assertions: false
  exclusions:
    rules:
      - path: _test\.go
        linters: [errcheck]
