version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - gosec
  settings:
    errcheck:
      check-type-assertions: false
  exclusions:
    rules:
      - path: _test\.go
        linters: [errcheck]
